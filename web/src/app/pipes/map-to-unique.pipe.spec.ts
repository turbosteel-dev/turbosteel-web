import { MapToUniquePipe } from './map-to-unique.pipe';

describe('MapToUniquePipe', () => {
  it('create an instance', () => {
    const pipe = new MapToUniquePipe();
    expect(pipe).toBeTruthy();
  });
});
